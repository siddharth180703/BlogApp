<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= blog.title %></title>
    <!-- Google Font: Poppins -->
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap"
      rel="stylesheet"
    />
    <!-- Tailwind CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body
    class="bg-gray-100 dark:bg-gray-900 font-poppins text-gray-900 dark:text-gray-100"
  >
    <div
      class="max-w-4xl mx-auto mt-10 bg-white dark:bg-gray-800 p-8 rounded-lg shadow-xl transform transition duration-300 hover:scale-105"
    >
      <h1
        class="text-4xl font-bold text-center text-blue-600 dark:text-blue-400 mb-6 hover:text-blue-400 transition-all ease-in-out"
      >
        <%= blog.title %>
      </h1>
      <div class="blog-cover-image mb-6">
        <img
          src="<%= blog.coverImageURL %>"
          alt="Cover Image"
          class="w-full h-72 object-cover rounded-lg shadow-md transition-all duration-500 hover:scale-105"
        />
      </div>
      <div class="blog-body text-lg leading-relaxed mb-6">
        <p><%= blog.body %></p>
      </div>
      <div
        class="blog-author flex items-center justify-between text-sm text-gray-600 dark:text-gray-400 mt-4"
      >
        <div>
          <p>Written by:</p>
          <p class="font-semibold text-blue-600 dark:text-blue-400">
            <%= blog.createdBy.fullName %>
          </p>
        </div>
        <div>
          <p class="italic text-gray-500 dark:text-gray-300">
            Posted on: <%= new Date(blog.createdAt).toLocaleDateString() %>
          </p>
        </div>
      </div>
      <div class="mt-8 text-center">
        <a
          href="/"
          class="bg-blue-600 text-white px-6 py-3 rounded-lg shadow-lg hover:bg-blue-700 transition duration-200 transform hover:scale-105"
        >
          Go Back to Home
        </a>
      </div>
      <!-- Comment Section -->
      <div class="mt-10 p-6 bg-gray-100 dark:bg-gray-700 rounded-lg">
        <h2
          class="text-2xl font-semibold text-gray-800 dark:text-gray-200 mb-4"
        >
          Comments
        </h2>
        <% if (comments.length > 0) { %>
        <div class="space-y-4">
          <% comments.forEach(comment => { %>
          <div class="p-4 bg-white dark:bg-gray-800 rounded-lg shadow-md">
            <p class="text-lg"><%= comment.content %></p>
            <p class="text-sm text-gray-500 dark:text-gray-400 mt-2">
              - <%= comment.createdBy.fullName %> on <%= new
              Date(comment.createdAt).toLocaleDateString() %>
            </p>
          </div>
          <% }); %>
        </div>
        <% } else { %>
        <p class="text-gray-500 dark:text-gray-400">
          No comments yet. Be the first to comment!
        </p>
        <% } %>
        <h2
          class="text-2xl font-semibold text-gray-800 dark:text-gray-200 mt-6"
        >
          Leave a Comment
        </h2>
        <form action="/blog/comment/<%= blog._id %>" method="POST">
          <div class="mb-4">
            <label class="block text-gray-700 dark:text-gray-300"
              >Comment</label
            >
            <textarea
              name="content"
              rows="4"
              class="w-full px-4 py-2 mt-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-600 dark:bg-gray-800 dark:border-gray-600 dark:text-gray-200"
              required
            ></textarea>
          </div>
          <button
            type="submit"
            class="bg-blue-600 text-white px-6 py-2 rounded-lg shadow-md hover:bg-blue-700 transition duration-200"
          >
            Submit Comment
          </button>
        </form>
      </div>
    </div>
    <footer class="bg-gray-800 text-white text-center py-6 mt-10">
      <p>&copy; 2025 BlogApp. All rights reserved.</p>
    </footer>
  </body>
</html>
