<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("./partials/head") %>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Blogs</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
    />
    <style>
      :root {
        color-scheme: light dark;
      }
      body.dark-mode {
        background: #121212;
        color: #e4e4e7;
      }
    </style>
  </head>
  <body
    class="bg-gradient-to-r from-gray-900 via-gray-800 to-gray-900 dark-mode min-h-screen p-10"
  >
    <%- include("./partials/script") %> <%- include("./partials/nav") %>
    <h1 class="text-5xl font-bold text-center text-gray-200 mb-10">
      Explore Blogs
    </h1>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% blogs.forEach((blog)=> { %>
      <div
        class="bg-white dark:bg-gray-800 rounded-xl shadow-lg transform transition duration-300 hover:scale-105"
      >
        <img
          src="<%= blog.coverImageURL %>"
          alt="<%= blog.title %>"
          class="w-full h-48 object-cover rounded-t-xl"
        />
        <div class="p-5">
          <h2
            class="text-2xl font-semibold text-gray-800 dark:text-gray-200 mb-3"
          >
            <%= blog.title %>
          </h2>
          <p class="text-gray-600 dark:text-gray-400 mb-4">
            <%= blog.body.length > 100 ? blog.body.substring(0, 100) + '...' :
            blog.body %>
          </p>
          <a
            href="/blog/<%= blog.id %>"
            class="block text-center bg-gradient-to-r from-blue-500 to-green-500 text-white font-bold py-2 px-4 rounded-lg shadow-md hover:from-blue-600 hover:to-green-600 transition duration-300"
            >Read More</a
          >
        </div>
      </div>
      <% }); %>
    </div>
  </body>
  <script>
    // Toggle dark mode if user prefers dark theme
    if (
      window.matchMedia &&
      window.matchMedia("(prefers-color-scheme: dark)").matches
    ) {
      document.body.classList.add("dark-mode");
    }
  </script>
</html>
